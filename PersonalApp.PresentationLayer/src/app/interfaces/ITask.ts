export interface ITask {
  taskId: number,
  userId: number,
  title: string,
  description: string,
  priority: string,
  status: string,
  dueDate: string,
  createdAt: string,
  completedAt: string
}
